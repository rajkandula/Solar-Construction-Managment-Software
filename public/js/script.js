function handleOptionClick(option) {
  // Get the selected value
  var selectedValue = option.value;

  // Perform the corresponding action based on the selected value
  if (selectedValue === "Approved") {
    // Perform the action for 'construction' option
    // Call the route or execute the desired logic
  } else if (selectedValue === "Rejected") {
    // Perform the action for 'manager' option
    // Call the route or execute the desired logic
  }
}
